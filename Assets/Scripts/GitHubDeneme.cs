using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GitHubDeneme : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //denemeGitHub
        
    }

    // Update is called once per frame
    void Update()
    {
       
    }
}